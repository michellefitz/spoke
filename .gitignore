# Xcode
# (** so the pattern also catches the xcworkspace nested inside the xcodeproj)
**/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods
Pods/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# OS
.DS_Store

# Local Claude session tooling
.planning/
.superpowers/
.impeccable.md

# Screen recordings and simulator captures (large media)
recordings/

# Secrets — copy Config.swift.example and fill in real keys locally
Spoke/Config.swift
